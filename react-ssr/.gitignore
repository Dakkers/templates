# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
build/
.output/
.nitro/
.tanstack/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
!.dev.vars.example
# Generated by `wrangler types` — regenerate locally, never commit.
worker-configuration.d.ts

# TypeScript
*.tsbuildinfo

# Environment (only *.local files are secret; committed .env files hold
# non-secret defaults)
.env.local
.env.*.local

# Vite
.vite/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
# Claude Code keeps checkouts of other branches here — never commit or format them.
.claude/worktrees/
*.swp
*.swo

# Test coverage
coverage/

# Scratch space — tracked as an empty dir via .gitkeep
.dummy/*
!.dummy/.gitkeep
